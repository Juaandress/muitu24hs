{
  "name": "muitu",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "deploy": "npx astro build && npx gh-pages -d dist"
  },
  "dependencies": {
    "@astrojs/check": "^0.7.0",
    "astro": "^4.10.3",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
